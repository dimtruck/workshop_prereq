import jenkins.model.*

def inst = Jenkins.getInstance()

def desc = inst.getDescriptor("hudson.plugins.git.GitSCM")

desc.setGlobalConfigName("{{ scm['global_config_name'] }}")
desc.setGlobalConfigEmail("{{ scm['global_config_email'] }}")

desc.save()