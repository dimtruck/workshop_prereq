jenkins_scripts:
  port: 8080
  cli_dest: '/var/lib/jenkins/jenkins-cli.jar' # Jenkins CLI destination
gerrit_host: localhost
gerrit:
  host_port: 35729
  user_name: jenkins
  user_email: jenkins@example.com
  user_key: /home/jenkins/.ssh/id_rsa
email_address: jenkins@example.com
ghprb:
  username: admin
  password: password
  access_token: token
  admin_list: admin
  published_url: http://localhost:8080
scm:
  global_config_name: jenkins
  global_config_email: jenkins@example.com
sonar:
  user_name: sonar-test
  url: http://localhost:9000,
  db: jdbc:h2:tcp://localhost:9092/sonar
  db_user: sonar
  db_password: sonar
  sonar_user: admin
  sonar_password: admin 